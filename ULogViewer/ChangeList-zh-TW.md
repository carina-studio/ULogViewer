# ULogViewer 2026.1 中有什麼改變
 ---

## 新功能
+ 支援使用鍵盤快捷鍵關閉應用程式 (macOS 上為 `⌘Q`，Windows/Linux 上為 `Ctrl+Q`)。

## 改善
+ 在 macOS 上編輯文字時，不再攔截 `⌘←` / `⌘→` 用於分頁切換。
+ 當有新版本時，在工具列上顯示按鈕以更新應用程式。

## 行為變更
+ 

## 錯誤修正
+ 其他錯誤修正。